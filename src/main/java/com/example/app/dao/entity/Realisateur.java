package com.example.app.dao.entity;

import java.time.LocalDate;

public class Realisateur extends Person{
    public Realisateur(String firstName, String lastName, LocalDate birthDate) {
        super(firstName, lastName, birthDate);
    }
}
